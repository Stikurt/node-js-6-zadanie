<!DOCTYPE html>
<html>
    <head>
        <title><%=title %</title>
        <meta charset="utf-8" />
    </head>
    <body>
        <h1> <% title %> в EJS</h1>
        <% if(emailsVisible) {%
        <h3>Электронная почта</h3>
        <ul>
            <% for(var i = 0; i<emails.length; i++) {%>
                <li><%=emails[i] %></li>}
            <%} %>    
        </ul>
    <% }else {%>
    <h3>Электронная адресс отсутсвует</h3>
    <%} %>
    <p>телефон: <%= phone %></p>
    <%- include('partials/footer/infoofme/fio.ejs') %>
    </body>
</html>